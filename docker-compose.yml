name: live-dev

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - ./backend/.env
    volumes:
      - pgdata:/var/lib/postgresql/data
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U livedev -d livedev"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Одно приложение: сайт + API + админка
  app:
    build: .
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      POSTGRES_HOST: db
      DEBUG: "0"
      FRONTEND_DIR: /app/frontend
      ALLOWED_HOSTS: live-dev.by,www.live-dev.by,localhost,127.0.0.1,app
      CSRF_TRUSTED_ORIGINS: https://live-dev.by,https://www.live-dev.by
      BEHIND_HTTPS_PROXY: "1"
    volumes:
      - media_data:/app/media
    depends_on:
      db:
        condition: service_healthy
    expose:
      - "8000"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8000/api/settings/ >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 10

  nginx:
    build:
      context: .
      dockerfile: deploy/nginx/Dockerfile
    restart: unless-stopped
    environment:
      DOMAIN: live-dev.by
    volumes:
      - media_data:/app/media:ro
    depends_on:
      app:
        condition: service_healthy
    ports:
      # TLS terminates on the server's shared public Nginx (ports 80/443).
      # This Nginx remains HTTP-only and efficiently serves assets and media.
      - "${LIVEDEV_HTTP_PORT:-8082}:80"
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  pgdata:
  media_data:
